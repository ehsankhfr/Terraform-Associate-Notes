### 🔹 Basics of Modules

1. What is a Terraform module?
   A) A command used to manage remote resources  
   B) A container for multiple resources used together  
   C) A feature for output validation  
   D) A remote backend for state files  

2. What is the root module in Terraform?
   A) A module from the registry  
   B) A reusable module  
   C) The main configuration in your working directory  
   D) The default provider  

3. How do you source a module from the local filesystem?
   A) `source = "./"`  
   B) `source = "local"`  
   C) `source = "./path/to/module"`  
   D) `source = "file://module"`  

4. What is the correct keyword to reference a module in your configuration?
   A) `resource`  
   B) `provider`  
   C) `module`  
   D) `data`  

5. Which of the following is **not** a valid module source?
   A) Terraform Registry  
   B) GitHub  
   C) Local path  
   D) Terraform Cloud Console  

---

### 🔹 Module Inputs

6. How do modules accept parameters?
   A) Using `locals`  
   B) Using `var.input`  
   C) Using `variable` blocks  
   D) Using `data` sources  

7. What is required to pass a value into a module?
   A) `locals` block  
   B) Input parameters inside the `module` block  
   C) A `provider` block  
   D) An `output` block  

8. In a module, where do you define default values for inputs?
   A) `locals.tf`  
   B) Inside `output` blocks  
   C) Inside `variable` blocks  
   D) You can’t set defaults in modules  

9. Which of the following is the correct way to assign a string input to a module?
   A) `vpc_name: my-vpc`  
   B) `vpc_name = "my-vpc"`  
   C) `set vpc_name = my-vpc`  
   D) `input.vpc_name = "my-vpc"`  

10. How are input variables referenced **inside** the module?
   A) `var.<name>`  
   B) `input.<name>`  
   C) `module.<name>`  
   D) `output.<name>`  

---

### 🔹 Module Outputs

11. What is the purpose of `output` blocks inside a module?
   A) To write logs  
   B) To return values from a module  
   C) To update state files  
   D) To expose provider settings  

12. How do you reference a module's output from the root module?
   A) `var.<output>`  
   B) `output.<module>.<name>`  
   C) `module.<module_name>.<output_name>`  
   D) `module.<output_name>`  

13. What happens if you don’t declare outputs inside a module?
   A) You can't pass inputs  
   B) The module fails to run  
   C) You cannot access any results from the module  
   D) Outputs are generated automatically  

14. Where do you typically define the output values from a module?
   A) In the `.tfvars` file  
   B) In the child module's `outputs.tf`  
   C) Inside the provider block  
   D) Inside the Terraform backend  

15. What is a use case for module outputs?
   A) Declare resource lifecycle  
   B) Configure provisioners  
   C) Pass values between modules  
   D) Configure backend encryption  

---

### 🔹 Meta-Arguments

16. What does the `count` argument do in a module block?
   A) Counts the number of output variables  
   B) Creates multiple instances of the module  
   C) Defines the max number of resources in the module  
   D) It is used for conditional logic  

17. When should you use `for_each` instead of `count`?
   A) When you need multiple copies with different parameters  
   B) When using a list of identical inputs  
   C) When defining lifecycle policies  
   D) When overriding provider credentials  

18. What does `depends_on` do in a module?
   A) Declares required provider versions  
   B) Ensures a specific order of operations  
   C) Declares module version  
   D) Declares output dependencies  

19. How do you pass a custom provider to a module?
   A) With the `providers` meta-argument  
   B) Inside the `backend` block  
   C) Using `depends_on`  
   D) With `provider_override`  

20. What happens if a module fails during creation due to a provisioner?
   A) The state file is removed  
   B) Terraform retries indefinitely  
   C) The resource is marked as tainted  
   D) Terraform exits without any action  

---

### 🔹 Advanced Use and Best Practices

21. How can modules be versioned when sourced from the Terraform Registry?
   A) Using the `version` argument  
   B) Using `terraform_version` inside the module  
   C) They cannot be versioned  
   D) Using the `backend` block  

22. Which of the following is a good practice for modules?
   A) Hardcode all values inside the module  
   B) Do not use variables or outputs  
   C) Keep modules small and single-purpose  
   D) Avoid using modules for production  

23. How can you organize modules in a repo?
   A) One module per `.tf` file  
   B) Nest them in the `.terraform` directory  
   C) Place each module in its own folder under `/modules`  
   D) All modules must be defined inline  

24. What is the advantage of using child modules?
   A) They simplify managing large configurations  
   B) They reduce code readability  
   C) They force re-creation of resources  
   D) They store Terraform state  

25. Can modules include other modules?
   A) No  
   B) Yes, using nested `module` blocks  
   C) Only with a local backend  
   D) Only using registry modules  

---

### 🔹 Real-World Scenarios

26. You want to reuse a VPC module in 3 different regions. What should you use?
   A) `for_each`  
   B) `count`  
   C) Separate Terraform workspaces  
   D) Remote state  

27. Which block exposes values like `vpc_id` or `subnet_ids` to the root module?
   A) variable  
   B) locals  
   C) provider  
   D) output  

28. If a module has an output like `output "id" { value = aws_instance.example.id }`, how would you access it in the root?
   A) `module.id`  
   B) `output.id`  
   C) `module.<module_name>.id`  
   D) `var.id`  

29. You want to create a reusable module for EC2 with instance type as input. What do you use?
   A) `locals`  
   B) `provider`  
   C) `variable "instance_type"`  
   D) `output "instance_type"`  

30. What does the following block represent?

```hcl
module "vpc" {
  source = "terraform-aws-modules/vpc/aws"
  name   = "my-vpc"
  cidr   = "10.0.0.0/16"
}
```

A) Resource definition  
B) Calling a remote VPC module from the registry  
C) Output block for a module  
D) Declaring a local variable  

---

| Q# | Answer | Explanation |
|----|--------|-------------|
| 1  | B | Modules group resources; they are reusable building blocks. |
| 2  | C | The root module is the code in your current working directory. |
| 3  | C | Local module paths are relative, like `./path/to/module`. |
| 4  | C | Use `module` block to call another module. |
| 5  | D | Terraform Cloud Console is not a module source. |
| 6  | C | Modules use `variable` blocks to define inputs. |
| 7  | B | Pass values via key-value pairs in the `module` block. |
| 8  | C | Default values go inside `variable` blocks using `default`. |
| 9  | B | Correct syntax is key = "value". |
| 10 | A | Inside a module, variables are accessed using `var.<name>`. |
| 11 | B | `output` blocks return values to the calling module. |
| 12 | C | Use `module.<module_name>.<output_name>` to access outputs. |
| 13 | C | Outputs are not available unless declared. |
| 14 | B | Outputs go in the module’s `outputs.tf` file. |
| 15 | C | Outputs pass values between modules or to root config. |
| 16 | B | `count` lets you deploy multiple copies of a module. |
| 17 | A | `for_each` supports keyed iteration with customization. |
| 18 | B | `depends_on` enforces order of resource/module creation. |
| 19 | A | `providers` block lets you override or assign custom providers. |
| 20 | C | Failed provisioners taint the resource for recreation. |
| 21 | A | `version = "x.y.z"` pins a module version from the registry. |
| 22 | C | Best practice is to keep modules simple and focused. |
| 23 | C | Use structured folders like `/modules/vpc` to organize. |
| 24 | A | Child modules modularize and simplify infrastructure code. |
| 25 | B | Modules can call other modules for deep reuse. |
| 26 | A | `for_each` lets you run a module for multiple regions. |
| 27 | D | Use `output` to return values like `vpc_id`. |
| 28 | C | Access output via `module.module_name.output_name`. |
| 29 | C | Declare input using `variable` in the module. |
| 30 | B | This calls a public module from the Terraform Registry. |
